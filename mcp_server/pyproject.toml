[project]
name = "videogame-collection-mcp"
version = "0.1.0"
description = "Cloudflare Access protected MCP adapter for VideogameCollection"
requires-python = ">=3.11"
dependencies = ["mcp>=1.26,<2", "httpx>=0.28,<1", "PyJWT[crypto]>=2.10,<3"]
